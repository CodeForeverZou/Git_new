git is a version control tool


git config --global user.name '..'
git config --global user.mail 

ssh-keygen
cd ~/.ssh
cat ira_ssh.pub
it remote add origin git@github.com:CodeForeverZou/Git2.git
===============================
工作区
git init
暂存区
git add <>

版本库
git commit -m '...'


================================
git log

git diff readme

git status

================================

git reset HEAD~~
git reflog

git reset Hard ead543f

git checkout -- readme
git reset HEAD readme

=================================
 git push -u origin master

echo "# Git2" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin git@github.com:CodeForeverZou/Git2.git
git push -u origin master

=========================
 git pull

 git branch dev
 git checkout master
 git branch -d dev

 ·
